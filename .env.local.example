# Solana Network Configuration
# Options: devnet, mainnet-beta
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Optional: Custom RPC Endpoint (if not specified, uses default public RPC)
# For production, consider using a dedicated RPC provider like Helius, QuickNode, or Alchemy
# NEXT_PUBLIC_RPC_ENDPOINT=https://api.devnet.solana.com

# Backend Keypair (Base58 encoded secret key)
# IMPORTANT: This keypair holds the mint authority for all created tokens
# Generate a new keypair using: solana-keygen new --outfile keypair.json
# Then convert to base58: cat keypair.json | jq -r '.' | tr -d '[],' | xargs echo | python3 -c "import sys; import base58; print(base58.b58encode(bytes([int(x) for x in sys.stdin.read().split()])).decode())"
#
# For testing on devnet, fund this wallet with SOL using: solana airdrop 2 <WALLET_ADDRESS> --url devnet
# Or use: https://faucet.solana.com/
#
# NEVER commit your actual secret key to git!
BACKEND_KEYPAIR_SECRET=your_base58_encoded_secret_key_here
